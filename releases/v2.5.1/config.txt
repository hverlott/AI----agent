# ========================================
# Telegram AI Bot - 功能开关配置 (v2.5.1 Template)
# ========================================
# 注意：本文件为默认模板。
# 多租户模式下，实际配置位于 data/tenants/{tenant_id}/platforms/telegram/config.txt
# ========================================
# 
# 说明：修改后立即生效，无需重启机器人
# 配置值：on 或 off（不区分大小写）
# ========================================

# 个人消息回复开关
# on = 开启（自动回复所有私聊消息）
# off = 关闭（不回复私聊消息）
PRIVATE_REPLY=on

# 群消息回复开关
# on = 开启（根据关键词和@触发回复）
# off = 关闭（不回复群聊消息）
GROUP_REPLY=off

# ========================================
# 内容审核系统配置 (Dual-Machine Audit)
# ========================================

# 审核开关 (on/off)
AUDIT_ENABLED=on

# 审核模式
# local  = 本地模式 (使用当前进程运行审核模型)
# remote = 远程模式 (调用外部审核服务，支持主备切换)
AUDIT_MODE=local

# 远程审核服务器地址 (仅在 AUDIT_MODE=remote 时生效)
# 多个地址用逗号分隔，系统将自动进行故障切换
# 示例: http://127.0.0.1:8000,http://192.168.1.100:8000
AUDIT_SERVERS=http://127.0.0.1:8000

# 审核最大重试次数
AUDIT_MAX_RETRIES=3

# 审核严格度 (0.0-1.0)
AUDIT_TEMPERATURE=0.0

# ========================================
# 其他配置（预留）
# ========================================

# 是否显示"正在输入"状态
# SHOW_TYPING=on

# 是否记录聊天日志
# LOG_MESSAGES=on
